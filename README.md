EcoBazaarX - Client

This is the React frontend for EcoBazaarX, a carbon-footprint aware shopping platform. Users can browse eco-friendly products, track their carbon footprint, and manage their cart through a profile drawer.

🛠️ Setup & Run
1. Clone the repository (if not already done):

   
   git clone <repository-url>

2. Navigate to the client folder:

   
   cd client

   
   cd Ecobazar
   
3. Install dependencies:

   
   npm install

 4. Start the development server:

    
    npm run dev

5. Open in browser:

    
   Go to http://localhost:5173    (default Vite port).
